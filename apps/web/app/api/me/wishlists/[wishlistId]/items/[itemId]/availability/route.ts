import { NextRequest,NextResponse } from "next/server";import { authenticatedApiRequest } from "../../../../../../../../features/auth/authenticated-api";
export async function GET(request:NextRequest,{params}:{params:Promise<{wishlistId:string;itemId:string}>}){const {wishlistId,itemId}=await params;try{const response=await authenticatedApiRequest(`/me/wishlists/${encodeURIComponent(wishlistId)}/items/${encodeURIComponent(itemId)}/availability?${request.nextUrl.searchParams.toString()}`);return new NextResponse(await response.text(),{status:response.status,headers:{"content-type":"application/json"}})}catch{return NextResponse.json({message:"Authentication is required."},{status:401})}}
